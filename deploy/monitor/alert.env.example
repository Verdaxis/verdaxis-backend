# Keep only enabled destination keys: retirement treats key presence as configured.
# Keep the operator-managed file root-only; the monitor readers never load it.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
HEALTHCHECKS_HEALTH_URL=
HEALTHCHECKS_BACKUP_URL=
