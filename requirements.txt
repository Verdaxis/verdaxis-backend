# Reproducible installs: the full transitive set is frozen in
# constraints.txt (pip install -r requirements.txt -c constraints.txt).
# Pinned: fastapi 0.139.x wraps included routers in lazy _IncludedRouter
# objects, breaking app.routes introspection (route-surface security tests
# and admin tooling). Re-test introspection before moving this pin.
fastapi==0.135.3
# Pinned at a release patched for CVE-2026-48710 (pip-audit clean); keep in
# lockstep with the fastapi pin above when either moves.
starlette==1.3.1
uvicorn
sqlalchemy[asyncio]>=2.0.0
asyncpg
alembic
geoalchemy2
pydantic>=2.0.0
pydantic-settings
email-validator
PyJWT>=2.8.0
bcrypt>=4.0.0
google-generativeai
pytest
pytest-asyncio
httpx
python-multipart
aiosqlite
psutil==5.9.4
sqladmin>=0.14.0
itsdangerous>=2.0.0
watchfiles
slowapi>=0.1.9
structlog>=24.0.0
feedparser
disposable-email-domains==0.0.225
free-email-domains==1.0.2
